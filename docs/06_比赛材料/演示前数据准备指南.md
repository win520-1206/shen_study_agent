# 比赛演示前数据准备指南

> 用途：比赛演示前清空测试数据，跑几次典型预设，让系统展示效果最干净、最有说服力。

---

## 第 1 步：备份当前数据库（以防万一）

在服务器上执行：

```bash
cp /opt/shen_study_agent/runtime/learnmate.db /opt/shen_study_agent/runtime/learnmate.db.bak
```

---

## 第 2 步：清空数据库

```bash
rm -f /opt/shen_study_agent/runtime/learnmate.db
systemctl restart learnmate
```

重启后系统会自动创建一个新的空数据库。

---

## 第 3 步：跑 3 个典型预设（在服务器上执行）

下面这 3 条命令模拟 3 种不同类型的学生，让系统有代表性数据。

### 零基础入门型

```bash
# 创建学生
STUDENT_ID=$(curl -s -X POST http://127.0.0.1:8000/api/v1/students \
  -H "Content-Type: application/json" \
  -d '{"name":"李同学","major":"人工智能","target_course":"机器学习基础"}' \
  | python3.13 -c "import sys,json; print(json.load(sys.stdin)['id'])")

echo "零基础学生 ID: $STUDENT_ID"

# 构建画像 + 生成资源
curl -s -X POST http://127.0.0.1:8000/api/v1/chat/profile-build \
  -H "Content-Type: application/json" \
  -d "{\"student_id\":$STUDENT_ID,\"message\":\"我是人工智能专业学生，机器学习零基础，希望通俗讲解，先建立整体认知，不用一开始就上很多代码。\"}" \
  | python3.13 -c "import sys,json; d=json.load(sys.stdin); print('画像:', d['student']['profile']['learning_goal']); print('资源数:', len(d['resources'])); print('推荐依据:', d['recommendation_summary'][:80])"

# 提交评估（模拟低分）
curl -s -X POST http://127.0.0.1:8000/api/v1/assessment/submit \
  -H "Content-Type: application/json" \
  -d "{\"student_id\":$STUDENT_ID,\"knowledge_unit\":\"线性回归\",\"score\":55}"
```

### 项目实战型

```bash
STUDENT_ID=$(curl -s -X POST http://127.0.0.1:8000/api/v1/students \
  -H "Content-Type: application/json" \
  -d '{"name":"王同学","major":"计算机科学","target_course":"机器学习基础"}' \
  | python3.13 -c "import sys,json; print(json.load(sys.stdin)['id'])")

echo "项目实战学生 ID: $STUDENT_ID"

curl -s -X POST http://127.0.0.1:8000/api/v1/chat/profile-build \
  -H "Content-Type: application/json" \
  -d "{\"student_id\":$STUDENT_ID,\"message\":\"我有 Python 和 sklearn 基础，想通过项目实战学会机器学习，重点做特征工程和模型评估的代码实验。\"}" \
  | python3.13 -c "import sys,json; d=json.load(sys.stdin); print('画像:', d['student']['profile']['learning_goal']); print('资源数:', len(d['resources'])); print('推荐依据:', d['recommendation_summary'][:80])"

curl -s -X POST http://127.0.0.1:8000/api/v1/assessment/submit \
  -H "Content-Type: application/json" \
  -d "{\"student_id\":$STUDENT_ID,\"knowledge_unit\":\"特征工程\",\"score\":75}"
```

### 考试冲刺型

```bash
STUDENT_ID=$(curl -s -X POST http://127.0.0.1:8000/api/v1/students \
  -H "Content-Type: application/json" \
  -d '{"name":"赵同学","major":"人工智能","target_course":"机器学习基础"}' \
  | python3.13 -c "import sys,json; print(json.load(sys.stdin)['id'])")

echo "考试冲刺学生 ID: $STUDENT_ID"

curl -s -X POST http://127.0.0.1:8000/api/v1/chat/profile-build \
  -H "Content-Type: application/json" \
  -d "{\"student_id\":$STUDENT_ID,\"message\":\"期末考试临近，我想快速掌握机器学习高频考点和易错题型，多给选择题和简答题练习。\"}" \
  | python3.13 -c "import sys,json; d=json.load(sys.stdin); print('画像:', d['student']['profile']['learning_goal']); print('资源数:', len(d['resources'])); print('推荐依据:', d['recommendation_summary'][:80])"

curl -s -X POST http://127.0.0.1:8000/api/v1/assessment/submit \
  -H "Content-Type: application/json" \
  -d "{\"student_id\":$STUDENT_ID,\"knowledge_unit\":\"模型评估\",\"score\":45}"
```

---

## 第 4 步：验证成果看板

```bash
curl -s http://127.0.0.1:8000/api/v1/overview/summary | python3.13 -m json.tool
```

预期结果：
- `student_count`: 3
- `most_common_weak_points`: 应该有至少 1 个薄弱点
- `resource_type_stats`: 应该有 5 种资源类型
- `featured_students`: 应该有 3 个学生案例

---

## 第 5 步：浏览器验证

打开 `http://121.199.26.183:8000`，确认：

1. 页面正常加载
2. 选择任意预设，点击生成
3. 看到"本轮推荐依据"卡片
4. 看到"教师/评委成果看板"有 3 个学生数据
5. 提交评估后，看板数据实时更新

---

## 注意事项

- 清空数据库后，之前的测试数据全部丢失
- 如果想恢复，用备份文件：`cp learnmate.db.bak learnmate.db && systemctl restart learnmate`
- 比赛当天建议提前 30 分钟跑一遍这个流程，确保数据状态最佳
