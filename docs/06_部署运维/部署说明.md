# 部署说明

## 本地开发

1. 安装 Python 3.11+
2. 执行 `pip install -r requirements.txt`
3. 执行 `uvicorn src.backend.app.main:app --reload`

## 前端开发

1. 进入 `src/frontend`
2. 执行 `npm install`
3. 执行 `npm run dev`

## 后续扩展建议

- 将 SQLite 升级为 MySQL
- 将显式规则编排升级为真实大模型调用
- 接入流式输出和鉴权
